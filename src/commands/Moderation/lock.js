module.exports = {
	name: "lock",
	description: "Prune amount of messages",
	aliases: new Set(["purge", "clean"]),
	category: "Moderation",
	usage: "<channel name><duration (optional)>(if the channel is already locked it unlocks it)",
	execute(message, args) {

	},
};
